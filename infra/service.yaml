apiVersion: v1
kind: Service
metadata:
  name: joao-marcos-api-recommendation-service
spec:
  selector:
    app: api-recommendation
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32188